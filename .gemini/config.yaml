!config
# Prioritize brand, template, and saga directories for context
search_paths:
  - "000-Global-Core/**"
  - "010-Valheim/010-Templates/**"
  - "010-Valheim/Chronicles-Of-The-Exile/**"

# Explicitly ignore unrelated system or junk files
ignore:
  - ".git/**"
  - "Thumbnail/**"

# Define the styleguide as the primary system instruction
system_instructions:
  file: ".gemini/styleguide.md"